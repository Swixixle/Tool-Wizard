# Tool-Wizard

## Live demo (GitHub Pages)

The **Guided Forge** static prototype lives in [`prototype/`](./prototype/). After a successful deployment from `main`, it is served at:

**https://swixixle.github.io/Tool-Wizard/**

Canonical entry: [`prototype/index.html`](./prototype/index.html) — Studio (Diagnostic → Library → Adapt → Preview).

Focused surfaces: [`prototype/diagnostic.html`](./prototype/diagnostic.html), [`prototype/library.html`](./prototype/library.html).

If this URL returns 404, confirm **Settings → Pages → Build and deployment → Source** is **GitHub Actions** (not “Deploy from a branch”) and that the “Deploy GitHub Pages” workflow has completed on `main`.