# Session 2026-04-29 (1) — Ignite the belt, ship what's there

**Goal:** Fire up the toolbelt for Tool-Wizard and walk through the four
priorities from the prior current-state (github fix → site → memory → push).

## What we did

1. **IGNITION-MCP not loading** — diagnosed: the venv at
   `~/Desktop/IGNITION/.venv` had a stale editable install whose `.pth`
   pointed at `~/IGNITION/src` (a *different* checkout, with a different
   package layout — sibling `ignition_mcp/` vs the new
   `ignition.mcp/` subpackage). Rebuilt the venv from scratch with
   `pip install -e ".[mcp]"`. Server connected on next Cmd-Q.
2. **github-mcp-server fix** — ran `fix-logs-cwd-pr.sh`. Clone succeeded,
   patch assertion failed: "expected AuditLogger call not found —
   already patched?" Read `src/index.ts` from origin/main via GitHub
   MCP; confirmed the fix is already in place (`resolveLogDir()` helper
   + XDG-based path). PR unnecessary. The previous "currently broken"
   memory was stale.
3. **Netlify deploy** — site already lived at
   `tool-wizard-chainwright.netlify.app` (site_id 5f82a677...). The MCP
   `deploy-site` op handed back a `npx @netlify/mcp` command; ran it
   from `site/`. Deploy `69f21ab4ba7e792b4f77a363` ready.
4. **Repo push** — local `~/My tool kit/Tool-Wizard/` wasn't a git
   repo, but `Swixixle/Tool-Wizard` already had a LICENSE + 13-byte
   placeholder README on main. Wrote `push-tool-wizard.sh` that:
   git-init → fetch origin → soft-reset HEAD to origin/main → restore
   LICENSE from origin → stage everything → commit → push. Landed as
   `8d3358f`.
5. **Repo-memory cwd** — pushed back when I tried to defer this; user
   was right that memory is the most important slot in the belt.
   Diagnostic chain: searched `~/Library/Application Support/Claude/`
   for `repo-memory` references; found the active config and an Apr 28
   backup. Backup had `"cwd": "/Users/alexmaksimovich/Open-Case"`;
   active config had no cwd at all. Server source
   (`~/repo-memory/dist/mcp/server.js`) doesn't honor env vars; cwd is
   the only knob. Wrote `wire-repo-memory.sh` that backs up the config,
   surgically adds `"cwd"` to the repo-memory entry, validates JSON,
   prints the new entry. Ran it pointed at Tool-Wizard.
6. **Cursor's parallel work** — landed independently while we were
   working: `prototype/` scaffolding (placeholder HTMLs + README),
   `.github/workflows/deploy-pages.yml`, merged README. Merge commit
   `ebc33aa`, workflow tweak `8e8a920`. Pages deploy blocked by a GitHub
   account billing issue (not a workflow bug).

## Surprises

- The github-mcp-server "broken" status was stale; fix had been merged.
- TWO IGNITION checkouts coexisted on disk; the editable install was
  pointing at the wrong one and confused the launcher script for hours.
- The dist `repo-memory` server has zero env-var awareness; cwd is the
  only mechanism. That makes project-switching a config-edit + restart
  ritual, not an env flip.
- The user has TWO deployment surfaces (Netlify + GitHub Pages) for two
  different things — that wasn't obvious until Cursor's prototype/
  workflow surfaced it.

## What's still open

- `prototype/index.html`, `diagnostic.html`, `library.html` are
  placeholder HTMLs — need real Cowork exports.
- GitHub billing block on Pages.
- repo-memory fix takes effect only after Cmd-Q + relaunch Claude.
- Chainwright classifier + library not yet implemented (the actual
  product work).
- Canon should be updated to reflect "monorepo: prototype/ inside
  Tool-Wizard" as the canonical structure.

## Closeout

- All session work pushed to origin/main.
- Three durable scripts now live at `~/My tool kit/`:
  `push-tool-wizard.sh` (idempotent — refuses if .git already exists),
  `wire-repo-memory.sh` (project-path arg, defaults to Tool-Wizard),
  `closeout-session.sh` (this script, end-of-session ritual).
- Next ignite (after Claude relaunch) should show repo-memory loading
  cleanly. If it doesn't, check that the running `node
  /Users/alexmaksimovich/repo-memory/dist/mcp/server.js` process has
  Tool-Wizard as its cwd (`lsof -p <pid> | grep cwd`).
